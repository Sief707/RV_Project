#ifndef CONFIG_HPP
#define CONFIG_HPP

#include <cstdint>

// ── Image dimensions ─────────────────────────────
// Change these two values to match your input image
static const uint32_t IMAGE_WIDTH  = 512;
static const uint32_t IMAGE_HEIGHT = 390;

// ── Input image path ─────────────────────────────
static const char* INPUT_IMAGE = "images/input.raw";

#endif